{
  "name": "chat-website-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd zenith-chat-main && npm install && VERCEL=1 npm run build && cd .. && mv zenith-chat-main/.vercel/output .vercel/output"
  },
  "engines": {
    "node": ">=20"
  }
}
